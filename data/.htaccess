# Prevent direct access to data files
<Files "*.json">
    Order Allow,Deny
    Deny from all
</Files>

# Prevent direct access to backup files
<Files "*.backup.json">
    Order Allow,Deny
    Deny from all
</Files>

# Prevent directory listing
Options -Indexes
